# Leave DATABASE_URL unset to use backend/smartpark.db automatically.
# From the backend directory, this explicit value resolves to the same file.
DATABASE_URL=sqlite:///./smartpark.db

# Add the deployed Vercel origin here in production. Local origins are always allowed.
FRONTEND_ORIGIN=http://localhost:5173
